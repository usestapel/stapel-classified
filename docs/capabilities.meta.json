{
  "_comment": "Hand-curated layer of docs/capabilities.json (capability-config.md §2 p.3). The emitter (stapel_classified/_capabilities.py) merges this over the derived axes; an axis in conf.py DEFAULTS missing here (or vice versa) is a loud emission error. Until 0.2.0 this file said the composite had no surface at all, which was true of a package that served no HTTP and owned no state. It served HTTP and owned one table from 0.2.0 to 0.3.1; since 0.3.2 the table is gone (stapel-chat 0.6.0 owns a conversation's subject) and the surface is a pure READ that joins three modules' answers into one header. The AXES are the settings that change what the product does to people: whether a block is enforced at all, and which providers answer the questions a conversation header asks. Batch limits, timeouts and image tiers are tuning and deliberately not axes. Deliberately NOT rooted in surface_roots: functions.py (comm surface, reached by name over the bus, never imported), checks.py (Django check callbacks), search_sources.py (resolved by dotted path out of STAPEL_SEARCH['SOURCES'], never called directly), preset.py (plain data a settings module copies).",
  "provides": "Composite for location-bound classified ads: the stapel-shop composite (categories + listings + reviews) plus stapel-geo, stapel-search and stapel-moderation, and the cross-domain declarations no member is allowed to write — the `listing` search source, the `listing`/`review`/`seller`/`chat_message` moderation target policies, the marketplace reason taxonomy, and (since 0.3.2) the `listing` subject type stapel-chat's registry ships empty of. Its own HTTP surface is a conversation HEADER, assembled from three modules that may not know about each other: chat says who is in the thread and what it is about, listings answers the short card (title, price, primary image with CDN render metadata, and a state that says `available`, `unavailable` or `gone` — exactly the answer a public read cannot give), profiles answers the counterparty. It owns NO table: the binding it kept from 0.2.0 to 0.3.1 existed only because chat could not tell two threads about two listings apart, and it was deleted rather than kept in sync the release chat could. It enforces a user-to-user block at the one place a classified contact begins, announcing at every boot whether that enforcement is live in this deployment.",
  "requires": [
    {
      "module": "stapel-categories",
      "optional": false,
      "reason": "composite member, inherited from the shop scenario (preset.py INSTALLED_APPS/URL_INCLUDES)"
    },
    {
      "module": "stapel-listings",
      "optional": false,
      "reason": "composite member — the classified ad itself (preset.py INSTALLED_APPS/URL_INCLUDES)"
    },
    {
      "module": "stapel-reviews",
      "optional": false,
      "reason": "composite member, inherited from the shop scenario (preset.py INSTALLED_APPS/URL_INCLUDES)"
    },
    {
      "module": "stapel-geo",
      "optional": false,
      "reason": "composite member — location binding for classified ads (preset.py INSTALLED_APPS/URL_INCLUDES)"
    },
    {
      "module": "stapel-attributes",
      "optional": false,
      "reason": "L1 library dependency of stapel-categories/stapel-listings; pip dependency, not a Django app (preset.py comment)"
    },
    {
      "module": "stapel-search",
      "optional": false,
      "reason": "composite member — the index, facets and geo radius over listings; the composite declares its `listing` source (preset.py SETTINGS_DEFAULTS, search_sources.py)"
    },
    {
      "module": "stapel-moderation",
      "optional": false,
      "reason": "composite member — the cross-target queue; the composite declares its `listing`/`review`/`seller`/`chat_message` target policies and the marketplace reasons (preset.py SETTINGS_DEFAULTS). 0.3.0+ is required: a chat message is the first target here whose content is PRIVATE, and 0.3.0 is where `can_view_content` is asked on behalf of the moderator actually looking"
    },
    {
      "module": "stapel-notifications",
      "optional": true,
      "reason": "not a member; when co-installed it routes the moderation letters (`listing_blocked`, `moderation.*`) the target policies name — absent, no letter is sent and nothing pretends one was"
    },
    {
      "module": "stapel-chat",
      "optional": false,
      "reason": "pinned (>=0.6) but not a member and never imported — it is ASKED, by name. Since 0.3.2 chat is the source of both halves of a conversation header: `chat.conversation_participants` answers who is in a thread and what it is about (0.6.0), and `chat.moderation_content` serves a reported message (0.5.0). Where no process serves those reads, a header answers 503 rather than an empty page and a complaint about a message answers 503 rather than a reporter's word for it"
    },
    {
      "module": "stapel-cdn",
      "optional": true,
      "reason": "reached by name (`cdn.describe_many`) for the card image's render metadata — the same contract stapel-chat uses for an attachment. Absent, the card keeps the image ref and says `meta_status: partial`"
    },
    {
      "module": "stapel-profiles",
      "optional": true,
      "reason": "reached by name for the counterparty card (`profiles.display_names`, `profiles.public_cards`) and for block enforcement (`profiles.relationships`, served since profiles 0.16.0). Absent, the seller card is `partial` — and because BLOCK_ENFORCEMENT defaults to `required`, a deployment without it must say so with `auto` or fail its own boot check (classified.E002)"
    }
  ],
  "extension_points": [
    {
      "name": "STAPEL_CLASSIFIED comm Function names",
      "kind": "settings",
      "summary": "Every read the conversation header makes is a comm Function NAME in this module's settings namespace — the listing documents, the display names, the public profile card, the seller rating, the CDN describe. Point one at your own provider and the answer changes with no fork and no release."
    },
    {
      "name": "STAPEL_MODERATION / STAPEL_SEARCH / STAPEL_REVIEWS declarations",
      "kind": "settings",
      "summary": "preset.SETTINGS_DEFAULTS is plain data: the search source, the four moderation target policies and the marketplace reason codes. All three registries merge over their built-ins, so a deployment adds a reason or removes a target type in its own settings rather than upstream."
    },
    {
      "name": "SerializerSeamMixin on every view",
      "kind": "subclass",
      "summary": "core's hoisted serializer seam: subclass a view, set request_serializer_class / response_serializer_class, remount the URL. No HTTP method body is ever copied."
    }
  ],
  "axes": {
    "BLOCK_ENFORCEMENT": {
      "business_label": "Is a block actually enforced",
      "summary": "Default `required` since 0.3.1: stapel-profiles 0.16.0 serves the block check, so a deployment without a block provider must SAY so rather than inherit a silent one — a missing provider is a boot error (E002). `auto` is that knowing statement: enforced wherever a provider answers, and where none is registered the deployment is told at every boot (W001). `off` is a disclosed statement (W002). In every state a provider that is present and FAILS answers 503, never 'allowed': an outage is not consent. The composite's preset sets the same `required` on stapel-chat, whose own default is `auto` because a generic chat may ship without profiles."
    },
    "BLOCK_FUNCTION": {
      "business_label": "Where a block is looked up",
      "summary": "The comm Function that answers 'is there a block between these two people'. A block belongs to the profile service and this composite keeps no copy of one; it asks, at the single place a classified conversation begins."
    },
    "PUBLIC_PROFILE_FUNCTION": {
      "business_label": "How much of a seller the card can show",
      "summary": "`profiles.public_cards`, served since stapel-profiles 0.16.0 — the avatar as the fleet image object, member-since, seller type. A deployment without profiles sets it empty and the counterparty card answers `partial` naming what is missing, rather than leaving a blank a client has to guess about."
    },
    "SELLER_RATING_TARGET_TYPE": {
      "business_label": "Whether sellers carry a rating",
      "summary": "Empty in the shipped scenario, because this composite registers reviews about listings rather than about people. A deployment that adds a seller review target names it here and the stars appear in every conversation header — with no release. Empty means the card says 'no rating', never a fabricated zero."
    },
    "CONVERSATION_PARTICIPANTS_FUNCTION": {
      "business_label": "Who is in a conversation, and what it is about",
      "summary": "The read that replaced this composite's own binding table in 0.3.2 (`chat.conversation_participants`, stapel-chat 0.6.0). It is the one seam here with no degraded form: an unreachable chat answers 503, because an empty page would be indistinguishable from 'you are not a party to any of these' and a reader would take an outage for a permission boundary."
    }
  },
  "surface_roots": [
    {
      "select": "functions",
      "path": "services.py"
    },
    {
      "select": "functions",
      "path": "cards.py"
    },
    {
      "select": "functions",
      "path": "blocks.py"
    }
  ],
  "surface": {
    "blocked_pairs": {
      "kind": "gate_function",
      "intent": "The batch form: which of these pairs are blocked, in one call.",
      "instead_of": [
        "calling is_blocked in a loop"
      ]
    },
    "chat_threads": {
      "kind": "factory",
      "intent": "Ask chat who is in these conversations and what each is about, batched — the read that makes a second copy of chat's membership unnecessary. Raises rather than answering empty when chat cannot be reached.",
      "instead_of": [
        "keeping your own copy of a conversation's participants",
        "importing stapel_chat models from a composite"
      ]
    },
    "confirm_listing_conversation": {
      "kind": "gate_function",
      "intent": "The 'write to the seller' moment, verified: the listing exists, the caller is not its seller, chat agrees the caller is in that thread and that it really is about that listing, and no block stands between the two. Answers the header the client is about to render, and records nothing.",
      "instead_of": [
        "storing a listing_id on your own chat model",
        "trusting the client's word for what a thread is about"
      ]
    },
    "conversation_context": {
      "kind": "factory",
      "intent": "One conversation's header: the short listing card (including sold and deleted, which is when a buyer is most confused) and the counterparty's public seller card, from the reader's side.",
      "instead_of": [
        "fetching the listing and the profile separately in a client"
      ]
    },
    "conversation_contexts": {
      "kind": "factory",
      "intent": "The batch form the inbox calls: a bounded page of headers in two comm reads, instead of one round trip per row.",
      "instead_of": [
        "looping conversation_context over a conversation list"
      ]
    },
    "is_blocked": {
      "kind": "gate_function",
      "intent": "Whether a block stands between two users, in either direction, under this deployment's declared enforcement posture. Raises rather than answering False when a configured provider fails.",
      "instead_of": [
        "querying a relationship table",
        "hiding the button in the UI"
      ]
    },
    "listing_cards": {
      "kind": "factory",
      "intent": "Keyed batch of short listing cards, gone ones included, with the CDN render metadata already merged over the primary image.",
      "instead_of": [
        "calling listings.search_documents and formatting a card yourself"
      ]
    },
    "party_ids": {
      "kind": "factory",
      "intent": "The user ids in one thread answer from chat_threads, as strings — the shape every authorization check here compares against.",
      "instead_of": [
        "reaching into the raw participants list in each caller"
      ]
    },
    "provider_unreachable_reason": {
      "kind": "gate_function",
      "intent": "Why the configured block provider cannot be called here, or an empty string — what the boot check prints and what tells the difference between 'no blocks in this deployment' and 'the block store is down'.",
      "instead_of": [
        "assuming a comm function is reachable"
      ]
    },
    "seller_cards": {
      "kind": "factory",
      "intent": "Keyed batch of PUBLIC seller cards — never more of a person than their public profile, whatever the caller holds.",
      "instead_of": [
        "reading a Profile row or a User row for a chat header"
      ]
    }
  }
}
